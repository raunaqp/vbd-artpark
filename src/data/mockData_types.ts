// Type re-exports so canonical.ts can avoid a circular import on mockData.ts.
export type { RegionData, HotspotData, OutbreakPrediction, RiskForecastPoint, StateId, DashboardFiltersLike } from "./mockData";
